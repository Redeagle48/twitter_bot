var later = require('later');

var sched = later.parse.recur().every(5).second(),
     t = later.setTimeout(test, sched);

var i = 3;

  function test() {
    console.log(new Date());
	i--;
	if(i >= 0){
		later.setTimeout(test, sched);
	}
  }